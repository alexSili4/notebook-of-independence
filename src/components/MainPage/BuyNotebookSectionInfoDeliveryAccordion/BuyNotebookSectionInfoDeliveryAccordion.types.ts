import { NumberOrNull } from '@/types/types';

export interface IStyledElementProps {
  isShowElement: boolean;
  scrollHeight: NumberOrNull;
}

export interface IStyledArrowIconProps {
  isShowElement: boolean;
}
